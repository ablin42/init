# Get the list of the network interfaces of the machine
# Without displaying any detail for these interfaces. Only the list of names.
#
ifconfig -l
#
# The -l flag may be used to	list all available interfaces on the system
# with no other additional information.
